# 双MCU通信问题
## 1. 满足要求
- 完整性 ：位翻转/丢帧/插帧/乱序/重复/粘连（stale）都要被检测到。
- 时效性 ：能检测“对方不在了/变慢了/卡住了”，并在规定时间内进入安全状态。
- 一致性 ：双方对“输入处理结果、状态机状态、将要输出的动作”必须可对比；任何超差要可证据化地触发安全反应。
- 无单点故障依赖 ：通信失败不能导致危险输出保持；设计上要做到“通信丢失→输出收敛到安全”。
- 可验证 ：：协议与实现能做故障注入/边界测试，并形成证据（SIL3 的关键）。
## 2. 传输的数据
- 互监测/互诊断：心跳、运行状态、输入结果处理、故障码、CRC
- 关键安全输入/状态/输出的结论一致
- 实现2oo2输出授权
- 通信出错就安全停机
## 3. SPI通信
板内高速、强确定性，适合“状态向量同步/投票”
### (1) 优点
- 高吞吐、低延迟、时序可控
- DMA好用
- 适合周期同步
### (2) 缺点
- 须有主从，双主容易总线争用
- SPI是单端短距离，对EMC地弹噪声敏感
### (3) 传输的数据
