安全功能要做什么 要达到什么要求（io延迟到多少秒超时）
# 会议问题
## 1 文档与交付
### 1.1 文档是否有模板
### 1.2 02 03文档的功能少不少

## 2. 软件编写前
### 2.1 HAL/LL使用 边界与工程基线
- 是不是允许 HAL 研发 如果允许，允许到什么程度
- 如果只能LL，那LL库的使用的生成的代码有哪些
### 2.2 版本冻结策略
- 冻结对象必须包含的有哪些 - 编译器版本、IDE版本、编译参数、链接脚本、启动文件、库版本、下载算法、诊断宏开关、脚本/CI环境
- 如何提供冻结证据：下载流程还是截屏
### 2.3 共因失效 对软件/硬件的具体要求
- 现在主从MCU的内部程序逻辑是一样的是否要做一些不同的设计来方式因为逻辑一样导致错误检测不出来

### 2.4 软件可配置设计
- 哪些关键参数需要配置
- MCU间的 通信频率 交换的数据
- 功能的安全等级在安全阈内可配置
- 安全等级的配置 3Level - 每一级需要包含的功能模块
- 
### 2.5 升级功能
- 是否需要包含升级功能
- 若需要最低的要求是什么

### 2.6 时间设计
- 上电到进入RUN间的时间消费是否有规定 若慧15s  西克18s
- 互监心跳行业的主流安排是什么
- 诊断周期如何确定 是开始前进行设计 还是 功能完成后通过测试收敛
- 诊断响应周期的确定
- 故障响应：约等于诊断周期+诊断响应周期  - T_detect + T_decide + T_actuate
这些时间是通过计算还是 行业主流的最后在测试后最终确定呢

### 2.7 安全功能的边界 
- 哪些是安全功能：诊断 日志 配置 通信SPI
- 哪些是非安全功能：CAN和RS485外部通信 
### 2.8 证据链的提供
- 评审认可的最小证据是什么 - 日志+时间戳是否足够 
- 
## 3. 软件编写与设计
### 3.1 自检库 + 证据
1) 自检的使用- 有无使用模板，有无注意点，需要规避
- 自检（POST & Run）允许的最长时间以及分片原则
2) 故障注入的实现
- post自检 运行期分片自检 的注意点
- 自检的时间 - 是否需要分片在post必须进行 的项目
- ISR注入哪些是必须要考虑的安全相关中断（时基、安全输入捕获、安全输出/反馈诊断、MCU互监心跳一致、看门狗）是否需要覆盖中断故障
3) 故障注入的证据如何获取与记录
- 采用示波器或者标志位检测吗
- 证据：示波器（t0/t1/t3）、内部时间戳、日志、构建产物（map/编译日志）、视频是否必要
4) 诊断要做到什么程度：时钟、RAM、Flash、CPU、堆栈、寄存器、总线、MPU/Cache 的检测策略

### 3.2 日志的显示
- 哪些事件必须记录才能支撑审查
### 3.3 编码器的设计
- 西克的速度监控主要是通过加速度超过一定阈值后进入leve0 我们的速度监控是否包含或者考虑其他的特征，是不是仅靠加速度监控就可以了
- 对于同源编码器双MCU读取是否需要同源输入异构计算

### 3.4 MCU的交互 仲裁
1) 心跳帧必须包含哪些字段才能支撑安全论证？
（计数器、时间戳、模式、故障码、CRC、版本等）
期望：最小字段集 + CRC/序号/超时模型。
2) 一致性对比做哪些内容？
（DI快照、编码器计算值、输出命令、故障状态、模式状态）
3) 通信的方式选择：SPI和USART
### 3.5 操作系统
- 以当前的工业主流方法以及后续的设计 是否需要或者建议上系统
### 3.5 安全通信
## 4. 后期工作
### 4.1. 工具可信度 - 工具链 的使用
1) 工具清单边界：
  - 项目包含的安全相关工具链：编译器/链接器/IDE/下载器/静态分析/单模块测试/覆盖率/CI等
2) 工具可信度线路：
- fusa资格化工具链+QKit   QKit的使用边界和限制如何写
- 继续使用AC6.15自证  需要补哪些TOR TVS TVR
3) 静态分析/动态验证工具
- 是否有推荐的组合 
- 是否有培训 教程 示例
