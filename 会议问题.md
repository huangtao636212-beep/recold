# 会议问题
## 1 文档与交付
### 1.2 文档是否有模板


## 2. 软件编写前
### 2.1 HAL/LL使用 边界与工程基线
- 是否允许 HAL 研发 如果允许，允许到什么程度（初始化 HAL_Delay HAL_ISR ）
- LL 的工程基线：是否要求“寄存器行为可审计、无隐藏状态机、可单测”？
期望输出：两者的边界规则 + 禁止清单 生成代码管理策略
### 2.2 版本冻结策略
- 冻结对象必须包含的有哪些 - 编译器版本、IDE版本、编译参数、链接脚本、启动文件、库版本、下载算法、诊断宏开关、脚本/CI环境
- 变更规则：哪些变更必须触发回归，回归的最小集是什么
### 2.3 目前对于双MCU功能实现主要采用的是镜像式的，是否需要引入差异化

### 2.4 软件可配置设计
- 哪些关键参数需要配置
- MCU间的 通信频率 交换的数据
- 功能的安全等级在安全阈内可配置
- 安全等级的配置 3Level - 每一级需要包含的功能模块
- 
### 2.5 升级功能
- 是否需要包含升级功能

### 2.6 时间设计
- 上电到进入RUN间的时间消费是否有规定 若慧15s  西克18s
- 互监心跳行业的主流安排是什么
- 诊断周期如何确定 是开始前进行设计还是安全功能模块基本测试完成然后选择最小的周期呢
- 诊断响应周期的确定
- 故障响应：~诊断周期+诊断响应周期  - 危险发生 -> 进入安全状态 T_detect + T_decide + T_actuate
这些时间是通过计算还是 行业主流的最后在测试后最终确定呢

## 3. 软件编写与设计
### 3.1 自检库 + 证据
1) 自检的使用- 有无模板，有无注意点，需要规避
2) 故障注入的实现
- post自检 运行期分片自检 的注意点
- ISR注入哪些是必须要考虑的安全相关中断（时基、安全输入捕获、安全输出/反馈诊断、MCU互监心跳一致、看门狗）是否需要进行一些中断 的故障注入以及检测到的证明ISR
3) 故障注入的证据如何获取与记录
- 采用示波器或者标志位检测吗
- 记录通过编译截屏 or 报错打印 or 视频 or map文件记录
4) 诊断要做到什么程度：时钟、RAM、Flash、CPU、堆栈、寄存器、总线、MPU/Cache 的检测策略

### 3.2 日志的显示
- 哪些事件必须记录才能支撑审查
### 3.3 编码器的设计
- 对于同源编码器双MCU读取是否需要同源输入异构计算
### 3.4 操作系统
- 以当前的工业主流方法以及后续的设计 是否需要或者建议上系统
## 4. 后期工作
### 4.1. 工具可信度 - 工具链 的使用
1) 工具清单边界：
  - 项目包含的安全相关工具链：编译器/链接器/IDE/下载器/静态分析/单模块测试/覆盖率/CI等
2) 工具可信度线路：
- fusa资格化工具链+QKit QKit的使用边界和限制如何写
- 继续使用AC6.15自证 需要补哪些TOR TVS TVR
3) 静态分析/动态验证工具
- 是否有推荐的组合 
- 是否有培训 教程 示例


## 9. MCU间的交互 仲裁

## 10. 编码器
- 速度规划 加速度不符合就进入level0呢，实时监控（）

## 14. 安全功能的边界 
- 哪些是安全功能：诊断 日志 配置 通信SPI
- 哪些是非安全功能：CAN和RS485外部通信 




KM:继电器
STO：驱动器



MISRA、单测、覆盖率、追溯工具



## 5.通信的内容 - 需要哪些数据，哪些环境

## 1. 刷新周期
## 2. 安全反应reaction
## 3. 通信的选择
## 4. 升级部分问题



## 3. 升级部分问题
### (1) 配置每个区的元数据状态
- DOWNLOADING（写入中）
- VERIFIED（CRC/Hash + 签名验证通过）
- TRIAL（下一次启动尝试）
- CONFIRMED（已确认长期有效）
- INVALID（失败/回滚）
### (2) 写入要升级的B
- 验证B：CRC \ Hash \ 签名验证（放置错误/篡改固件被刷进去）
- 设置升级标志位，重启启动B
- 对B进行POST + 安全方面的自检 
	- 满足成功条件，写入确认成功标志 - 下次上电就固定是B 
	- 失败回滚到A
### (3) 升级的状态是维修模式
### (4) 升级进行方式
- 可以任何地方发起，但必须等系统进入可升级窗口（满足升级条件）才进行升级

## 4. 刷新周期
- 高频（Safety Kernel ）：4 ms
- 中频（运行诊断/一致性）：20ms
- 低频（维护数据/自检）：200ms
## 5. MCU区分
### 5.1 同构方式
#### (1) 两颗 MCU 跑同一套应用，差异
- BSP 引脚映射不同（DI/DO/ADC/EN/CS 等） ≈ 0
- 是否要相同呢，例如spi连着相同的引脚会不会导致同一危险输出、总线抢占、双主冲突

- 每个可升级镜像（给 MCU1 / MCU2 的）都要有一个不可伪造的头，避免“刷错对象 / 刷错包”

## 6. LL库选择与配置方法
- CubeMX 只负责：
	- GPIO的复用/ 时钟树 / DMA 资源分配
	- 生成 MX_GPIO_Init()、MX_ADCx_Init() 等 初始化底层（最好选 LL 风格）